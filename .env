# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Connection Details
# Use localhost since you are running the server on Debian, not inside Docker
DATABASE_URL=postgres://esop_admin:secure_password_123@localhost:5432/esop_management

# Database Credentials
DB_USER=esop_admin
DB_PASSWORD=secure_password_123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=esop_management

# Authentication Secret
JWT_SECRET=your_super_secret_esop_key_2026